name: vite Docker Push
on: 
  push: 
    branches: [ "master" ]
  
jobs:
  Viteapp-Test:
   
  
    name: Build Docker Image
    runs-on: ubuntu-latest  
    steps:  
        - name: Checkout
          uses: actions/checkout@v2    
          
        - name: Setup AWS ECR Details
          uses: aws-actions/configure-aws-credentials@v1
          with:
            aws-access-key-id: ${{ secrets.AKIAQLSIVEOGMSDZKIEV }}
            aws-secret-access-key: ${{ secrets.HUe/NUXe9Ta4a37KGQC9WV17gOtE/t9x20Bdjt1g }}
            aws-region: ${{secrets.ap-southeast-2}}


        - name: Login to Amazon ECR
          id: aws ecr get-login-password --region ap-southeast-2 | docker login --username AWS --password-stdin 024848442252.dkr.ecr.ap-southeast-2.amazonaws.com
          uses: aws-actions/amazon-ecr-login@v1
          

        - name: Build and push the tagged docker image to Amazon ECR
          env:
            ECR_REGISTRY: ${{ steps.login-pf-aws-ecr.outputs.registry }}
            ECR_REPOSITORY: ${{secrets.AWS_ECR_REPO}}
            IMAGE_TAG: latest
          run: |
            docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
            docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
    